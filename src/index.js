import React from 'react';
import ReactDOM from 'react-dom/client';
import Add from './Add';
import './index.css';
import Parentcount from './Parentcount';
import StyleComponents from './stylecomp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <StyleComponents/>
  </React.StrictMode>
);



